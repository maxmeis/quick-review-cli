#!/usr/bin/env bash
# Development launcher. For a standalone binary: make build or go install ./cmd/review
set -euo pipefail
project_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
exec go -C "$project_dir" run ./cmd/review "$@"
